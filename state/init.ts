// functions to intialize the app
import { zen, distributor } from "../contracts";

export async function initApp() {
  // fetch contract info
  // console.log("initializing app");
  //   const totalSupploy = await zen.totalSupply();
  //   const cap = await distributor.cap();
  // const capLeft = await distributor["capLeft"]();
  //   console.log("totalSupploy", totalSupploy);
  //   console.log("cap", cap);
  // console.log("capLeft", capLeft);
}
